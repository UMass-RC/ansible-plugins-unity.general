todos:
  image: alpine
  script:
    - apk add ripgrep
    - set -x
    - set +e
    - todo_count="$(rg TODO | wc -l)"
    - curl "https://shields.io/badge/TODOs-$todo_count-red" > badge.svg
  artifacts:
    paths:
      - badge.svg
    expire_in: 1 week
fixmes:
  image: alpine
  script:
    - apk add ripgrep
    - set -x
    - set +e    
    - fixme_count="$(rg FIXME | wc -l)"
    - curl "https://shields.io/badge/FIXMEs-$fixme_count-red" > badge.svg
  artifacts:
    paths:
      - badge.svg
    expire_in: 1 week