todos:
  image: alpine
  script:
    - apk add ripgrep curl
    - set +e
    - todo_count="$(rg TODO | wc -l)"
    - set -e
    - curl "https://shields.io/badge/TODOs-$todo_count" > todos.svg
  artifacts:
    paths:
      - todos.svg
    expire_in: 1 week
fixmes:
  image: alpine
  script:
    - apk add ripgrep curl
    - set +e    
    - fixme_count="$(rg FIXME | wc -l)"
    - set -e
    - curl "https://shields.io/badge/FIXMEs-$fixme_count" > fixmes.svg
  artifacts:
    paths:
      - fixmes.svg
    expire_in: 1 week