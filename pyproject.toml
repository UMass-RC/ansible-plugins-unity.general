[project]
name = "general"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = "==3.12"
dependencies = [
    "ansible<12",
    "pyexpect",
    "clustershell"
]
